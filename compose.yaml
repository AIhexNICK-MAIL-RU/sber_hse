services:
  workflow-copilot-api:
    build:
      context: ./workflow_compactor
      dockerfile: Dockerfile
    container_name: workflow-copilot-api
    ports:
      - "8000:8000"
    restart: unless-stopped
